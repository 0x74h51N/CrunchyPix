export type slide = {
  imageUrl: string;
  title: string;
  description: string;
  left: boolean;
  active?: boolean;
};

export type RobustSection = {
  title: string;
  description: string;
  icon: string;
};
